# Soluções Cyber

Client-side recommender for cybersecurity market solutions. Answer a few
questions — which category you need (EDR, SIEM, CMDB, WAF…), company size,
budget, ecosystem and compliance — and get a ranked shortlist of solutions that
fit your scenario, with a "why it fits" rationale, **entirely in the browser**.
There is no backend.

Part of the [project hub](https://sergiogbernardo.github.io/), alongside
[Headersvg](https://sergiogbernardo.github.io/Headersvg/) and
[Scanvg](https://sergiogbernardo.github.io/Scanvg/).

## How it works

Two views, switchable by tab:

- **Catálogo** (main) — the full, browsable catalog of every solution. Search by
  product, vendor or category, filter by area and sort by market rating or name.
- **Encontrar solução** — a guided wizard, one question per matching dimension,
  that returns a ranked shortlist for your scenario.

Under the hood:

- **Matching engine** (`src/lib/match.ts`) — scores each solution 0–100 from:
  market rating, company-size↔tier fit, budget↔price fit, ecosystem match and
  compliance keywords. The market rating breaks ties. Pure, tested TypeScript.
- **Results** — top 5 solutions with reasons (✓) and caveats (!), price tier and
  market rating.

## Catalog

The catalog lives at `src/data/solucoes.json` and is bundled at build time — the
"API" is just a static file fetched into the browser.

It is generated by `scripts/normalize.mjs` from two sources:

- `scripts/source/tools-seed-data.json` — vendored snapshot of the market
  solutions from the `cyber-architecture` project.
- `scripts/extra-solutions.json` — solutions added by hand (e.g. CMDB). Edit
  this file to extend the catalog, then regenerate.

```bash
npm run data   # regenerates src/data/solucoes.json
```

> Market ratings come from Gartner Peer Insights / G2 where catalogued. They are
> a quality signal, **not** an official Magic Quadrant position. Solutions added
> by hand carry `marketRating: null` until a real source is filled in — we do not
> invent ratings.

## Stack

React + TypeScript + Vite + Tailwind. Matching and scoring are plain TypeScript,
tested with Vitest. No backend, no tracking.

## Develop

```bash
npm install
npm run dev
```

## Test, lint, build

```bash
npm test           # vitest — matching engine
npm run lint
npm run build      # outputs to dist/
npm run preview
```

The Vite `base` is `/solucoes-cyber/` to match GitHub Pages. Deployment is
automated by `.github/workflows/deploy.yml` on every push to `main` (it runs
lint + tests before building).
